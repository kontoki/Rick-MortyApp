import spinner from "../../resourses/icons/spinner.gif";

const Spinner = () => {
  return (
    <img
      src={spinner}
      alt="spinner"
      style={{
        display: "flex",
        margin: "150px auto",
      }}
    />
  );
};

export default Spinner;
